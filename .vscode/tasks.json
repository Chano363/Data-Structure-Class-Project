{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "cmake --build ${workspaceFolder}/build",
            "group": "build",
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "使用 CMake 构建项目"
        }
    ]
}